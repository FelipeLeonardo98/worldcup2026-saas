version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static MVP - no frontend build step required"
  artifacts:
    baseDirectory: .
    files:
      - index.html
      - styles.css
      - script.js
      - README.md
      - assets/**/*
  cache:
    paths: []
